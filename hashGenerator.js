"use strict";

const symbols = 'abcdefghijklmnopqrstuvwxyz1234567890';


function generator() {
    let hash = '';
    for (let i = 0; i<= 16; i++) {
        for(let a of symbols) {
            hash[i] = a
            for(let b of symbols) {
                let hash1 = ''

                console.log('hash1 ', )
            }
        }
    }
}
